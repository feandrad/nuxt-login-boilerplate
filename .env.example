# Dev, homolog ou prod
APP_ENV=development

# URL do projeto no Supabase (copie de Settings → API → Project URL)
SUPABASE_URL=https://your-project-id.supabase.co
# Chave ANON (pública) — usada no client (Nuxt browser)
SUPABASE_KEY=your-anon-key-here
# Chave SERVICE (privada) — usada apenas no server (Nuxt server routes / API)
SUPABASE_SERVICE_KEY=your-service-role-key-here
